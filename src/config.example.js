const config = {
	ownerID: 'OWNER-ID',
	token: 'BOT-TOKEN',
	client: 'BOT-CLIENT',
	BotID: 'BOT-ID',
	// or http
	protocol: 'https',
	// include the protocol
	domain: 'domain',
	sessionSecret: 'JUST SPAM YOUR KEYBOARD',
};

module.exports = config;
